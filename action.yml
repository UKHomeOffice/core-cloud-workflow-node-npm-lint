name: Node NPM Lint

inputs:
  working_directory:
    description: "Directory to run NPM lint in (default: repository root)"
    required: false
    type: string
    default: "."

  node_version:
    description: "Node version to install (e.g., 24)"
    required: false
    type: string
    default: "24"

outputs:
  npm_lint_exit_code:
    description: "The exit code from the npm lint command"
    value: ${{ steps.npm-lint.outputs.exit_code }}

runs:
  using: "composite"

  steps:
    - name: Checkout Repository
      uses: actions/checkout@v6

    - name: Setup Node
      uses: actions/setup-node@v6
      with:
        node-version: ${{ inputs.node_version }}

    - name: Install Node Dependencies
      working-directory: ${{ inputs.working_directory }}
      run: npm ci

    - name: Run NPM lint
      id: npm-lint
      shell: bash
      working-directory: ${{ inputs.working_directory }}
      run: |
        npm run lint

        EXIT_CODE=$?
          echo "exit_code=$EXIT_CODE" >> $GITHUB_OUTPUT
